You are an expert prompt engineer.
Please generate a GENERATING-PROMPT that transforms the ORIGINAL-TEXT into a GENERATED-TEXT.

    ORIGINAL-TEXT   METADATA
        |               |
        +-------+-------+
                |
                v
    GENERATING-PROMPT (generate this)
                |
                v
    GENERATED-TEXT (must satisfy the following [Rubrics])

The GENERATING-PROMPT must be written in "__RUBRICS_LANG__" (ISO 639-1).
The GENERATED-TEXT produced by the GENERATING-PROMPT must satisfy the following [Rubrics].

[Structure]

Use the following structure for the GENERATING-PROMPT:
Do not rename the section names in the input section.

    > ## task
    >
    > Describe the task.
    >
    > ## requirements
    >
    > Requirements for satisfying the following [Rubrics].
    >
    > ## input
    >
    > ### __SECTION_ORIGINAL__
    >
    > __PLACEHOLDER_ORIGINAL__
    >
    > ### __SECTION_METADATA__
    >
    > __PLACEHOLDER_METADATA__

Only the input section should contain placeholders.
They will be replaced with the ORIGINAL-TEXT and its metadata during post-processing.
Elsewhere, refer to them using natural language.

[Rubrics]

__JSON__

[Note]

Return only the GENERATING-PROMPT.
Do not explain your reasoning.
Do not wrap the response in code fences.
